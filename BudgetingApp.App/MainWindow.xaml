<Window x:Class="BudgetingApp.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Budgeting" Height="800" Width="1200"
        Style="{StaticResource AppWindowStyle}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
            <DockPanel Margin="0,20,0,20">
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="18,0,0,24">
                    <Polygon Points="6,0 12,6 6,12 0,6" Fill="{StaticResource AccentBrush}" Width="12" Height="12" Effect="{StaticResource AccentGlowEffect}" VerticalAlignment="Center"/>
                    <TextBlock Text="BUDGETING" FontFamily="{StaticResource DisplayFontFamily}" FontSize="18" FontWeight="Bold"
                               Foreground="{StaticResource SidebarTextBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Top">
                    <Button x:Name="ImportNavButton" Content="Import CSV" Style="{StaticResource ActiveNavButtonStyle}" Click="ImportNavButton_Click" HorizontalAlignment="Stretch"/>
                    <Button x:Name="AccountsNavButton" Content="Accounts" Style="{StaticResource NavButtonStyle}" Click="AccountsNavButton_Click" HorizontalAlignment="Stretch"/>
                    <Button x:Name="TransactionsNavButton" Content="Transactions" Style="{StaticResource NavButtonStyle}" Click="TransactionsNavButton_Click" HorizontalAlignment="Stretch"/>
                    <Button x:Name="RecurringNavButton" Content="Recurring Expenses" Style="{StaticResource NavButtonStyle}" Click="RecurringNavButton_Click" HorizontalAlignment="Stretch"/>
                    <Button x:Name="RulesNavButton" Content="Category Rules" Style="{StaticResource NavButtonStyle}" Click="RulesNavButton_Click" HorizontalAlignment="Stretch"/>
                    <Button x:Name="SummaryNavButton" Content="Monthly Summary" Style="{StaticResource NavButtonStyle}" Click="SummaryNavButton_Click" HorizontalAlignment="Stretch"/>
                    <Button x:Name="TrendsNavButton" Content="Trends" Style="{StaticResource NavButtonStyle}" Click="TrendsNavButton_Click" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <ContentControl x:Name="ContentArea" Grid.Column="1" Margin="28"/>
    </Grid>
</Window>
