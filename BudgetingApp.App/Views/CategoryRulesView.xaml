<UserControl x:Class="BudgetingApp.App.Views.CategoryRulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Category Rules" Style="{StaticResource PageHeaderStyle}"/>

        <Border DockPanel.Dock="Top" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="A transaction is auto-categorized by the first matching rule, in priority order (lowest number first). No rule can point at more than one category." Style="{StaticResource SectionLabelStyle}" TextWrapping="Wrap"/>
                <WrapPanel Margin="0,8,0,0">
                    <StackPanel Margin="0,0,12,0" Width="150">
                        <TextBlock Text="Category" Style="{StaticResource SectionLabelStyle}"/>
                        <ComboBox x:Name="CategoryBox" DisplayMemberPath="Name" SelectedValuePath="Id"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,12,0" Width="110">
                        <TextBlock Text="Match Type" Style="{StaticResource SectionLabelStyle}"/>
                        <ComboBox x:Name="MatchTypeBox"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,12,0" Width="220">
                        <TextBlock Text="Pattern" Style="{StaticResource SectionLabelStyle}"/>
                        <TextBox x:Name="PatternBox"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,12,0" Width="90">
                        <TextBlock Text="Priority" Style="{StaticResource SectionLabelStyle}"/>
                        <TextBox x:Name="PriorityBox" Text="100"/>
                    </StackPanel>
                    <StackPanel VerticalAlignment="Bottom">
                        <Button Content="Add Rule" Style="{StaticResource AccentButtonStyle}" Click="AddButton_Click"/>
                    </StackPanel>
                </WrapPanel>
                <TextBlock x:Name="ValidationText" Foreground="{StaticResource NegativeBrush}" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="RulesGrid" DockPanel.Dock="Top">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="70" IsReadOnly="True"/>
                <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="140" IsReadOnly="True"/>
                <DataGridTextColumn Header="Match Type" Binding="{Binding MatchType}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Pattern" Binding="{Binding Pattern}" Width="*" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Style="{StaticResource SecondaryButtonStyle}" Padding="8,4" Click="DeleteButton_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
