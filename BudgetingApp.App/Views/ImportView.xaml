<UserControl x:Class="BudgetingApp.App.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Import CSV" Style="{StaticResource PageHeaderStyle}"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,16">
            <StackPanel Margin="0,0,12,0" Width="180">
                <TextBlock Text="Account" Style="{StaticResource SectionLabelStyle}"/>
                <ComboBox x:Name="AccountBox" DisplayMemberPath="Name" SelectedValuePath="Id"/>
            </StackPanel>
            <StackPanel VerticalAlignment="Bottom">
                <Button Content="Choose CSV File..." Style="{StaticResource AccentButtonStyle}" Click="ChooseFileButton_Click"/>
            </StackPanel>
        </StackPanel>

        <CheckBox x:Name="FlipSignCheckBox" DockPanel.Dock="Top" Margin="0,0,0,16" VerticalAlignment="Top">
            <TextBlock TextWrapping="Wrap" MaxWidth="600">
                This file shows a charge/expense as a <Run FontWeight="SemiBold">positive</Run> number
                (common on dedicated credit card statements). Leave unchecked if expenses are already
                negative, same as a normal checking export.
            </TextBlock>
        </CheckBox>

        <Border DockPanel.Dock="Top" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                Padding="16" Margin="0,0,0,16" Visibility="Collapsed"
                x:Name="ResultPanel">
            <StackPanel>
                <TextBlock x:Name="ResultSummaryText" FontWeight="SemiBold" FontSize="14"/>
                <TextBlock x:Name="ResultErrorHeaderText" Margin="0,12,0,4" Foreground="{StaticResource NegativeBrush}"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="RowErrorsGrid" DockPanel.Dock="Top" MaxHeight="300">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Row" Binding="{Binding RowNumber}" Width="60"/>
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="260"/>
                <DataGridTextColumn Header="Raw Line" Binding="{Binding RawLine}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
