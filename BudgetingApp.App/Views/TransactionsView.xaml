<UserControl x:Class="BudgetingApp.App.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Transactions" Style="{StaticResource PageHeaderStyle}"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <CheckBox x:Name="UncategorizedOnlyCheckBox" Content="Uncategorized only" VerticalAlignment="Center" Checked="FilterChanged" Unchecked="FilterChanged" Margin="0,0,20,0"/>
            <TextBlock Text="Account:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox x:Name="AccountFilterBox" Width="160" DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="FilterChanged" Margin="0,0,20,0"/>
            <Button Content="Re-run categorization" Style="{StaticResource SecondaryButtonStyle}" Click="RerunCategorizationButton_Click"/>
        </StackPanel>

        <DataGrid x:Name="TransactionsGrid" DockPanel.Dock="Top">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding PostingDate}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="100" IsReadOnly="True"/>
                <DataGridComboBoxColumn Header="Category" x:Name="CategoryColumn" Width="180"
                                         SelectedValueBinding="{Binding CategoryId}" SelectedValuePath="Id" DisplayMemberPath="Name"
                                         EditingElementStyle="{StaticResource DataGridComboBoxEditingStyle}"/>
                <DataGridTextColumn Header="Account" Binding="{Binding AccountName}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Source File" Binding="{Binding SourceFile}" Width="140" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
