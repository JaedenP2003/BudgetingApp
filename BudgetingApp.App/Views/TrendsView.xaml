<UserControl x:Class="BudgetingApp.App.Views.TrendsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Trends" Style="{StaticResource PageHeaderStyle}"/>

        <Border DockPanel.Dock="Top" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                Padding="16" Margin="0,0,0,16" x:Name="NoDataPanel" Visibility="Collapsed">
            <TextBlock Text="No data imported yet." FontSize="14" Foreground="{StaticResource MutedTextBrush}"/>
        </Border>

        <Grid x:Name="ContentGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="12" Margin="0,0,0,16">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Income, expenses, and savings by month" Style="{StaticResource SectionLabelStyle}" Margin="0,0,0,8"/>
                    <lvc:CartesianChart x:Name="OverviewChart" Background="Transparent"/>
                </DockPanel>
            </Border>

            <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="12">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Category:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="CategoryBox" Width="180" DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="CategoryBox_SelectionChanged"/>
                    </StackPanel>
                    <lvc:CartesianChart x:Name="CategoryTrendChart" Background="Transparent"/>
                </DockPanel>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
